.claude/

node_modules/
dist/
.astro/


# Real customer billing exports — local sample data only. Kept out of the
# repo entirely (they contain billing-account IDs and customer names), even
# though src/docs/ is never reached by the build (see src/docs/ARCHITECTURE.md).
src/docs/csv/
*.csv
